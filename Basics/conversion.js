let score = "12"
console.log(typeof(score))

let value = Number(score)
console.log(typeof(value))

// converting a string with not only Integers using Number() gives NaN(i.e not a number) , & it is also of number type.
