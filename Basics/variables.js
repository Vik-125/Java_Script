const accountId = 12      //  const fixez the value, & we can't change the value of the variable.
let accEmail = "abc@gmail.com"
var accPass = "123"
accCity = "jaipur"

//accountId = 2

console.log(accountId);

// prefer not to use var , due to block scope problem.
 
console.table([accountId, accEmail, accPass])